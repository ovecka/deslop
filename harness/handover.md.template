# Handover — <project> — <date>

## Acceptance criteria (negotiated at minute 0)
- Production run means: <deploy target / path judgment>
- Data classification: <internal tool / customer data / payment flow>
- Slot length: <min> → <N> discovery sweeps
- Main path (stakeholder's words, whole pipeline input → output, not README's first command): <stage → stage → …>
- Already decided about the product (ask the stakeholder BEFORE the first finding — a fix
  built on an assumed product is wasted iterations): <e.g. "no anonymous tier, all AI
  calls behind login, budget per user">
- Third-party egress (what leaves, to whom, decided?): <e.g. "portfolio tickers → LLM vendor: ok; customer names → LLM: NOT decided">
- Platform-generated: <no / Bolt|Lovable|n8n… — source of truth, export, deploy lives in platform>

## Baseline (what's true)
- Runs: <yes / DEGRADED MODE — gate is static-only, severities marked unverified>
- Green: <tests / lint / typecheck / lockfile state>
- Main path trace:

| stage | command | input → output | network / secret |
|-------|---------|----------------|------------------|

## My predictions before discovery (eval input)
- <prediction 1>
- <prediction 2>

## Findings → commits (audit trail)
| Finding-Id | file:line | severity | commit | verdict |
|------------|-----------|----------|--------|---------|

## Verdict
- <productionize (fixes below) / do not productionize — replace with <X> / retire, because <reason>>

## Deferred (with reasons)
- <finding — reason for deferral>

## Path to production (ordered — incl. what this session didn't get to)
1. <env config / health endpoint / logs / deploy steps / rollback / monitoring>

## For the app author (non-IT, human language)
- What changed and why: <plain sentences, no jargon>
- What still needs a decision from you: <...>

## Eval loop closure
- Predictions vs. agent reports — divergences: <...>
- Rubric/CLAUDE.md patch taken from this session: <...>
- Session cost (approx): <tokens / $>

## Next concrete step
- <one action a fresh session starts with>
