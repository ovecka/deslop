# Agent evals — <project> — <date>

Every fuckup is a harness bug. Measure it, patch the harness, note the patch.

| Fix (Finding-Id) | Passed 1st try | Iterations | CLAUDE.md violation | Harness patch |
|------------------|---------------|------------|--------------------|---------------|

Retry cap: 3 iterations per fix, then back to triage and swap for another finding.
